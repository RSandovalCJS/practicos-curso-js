Se agrega archivo app.js con funcionalidad en boton para flotante que permite un "scroll" suave que sube al inicio de la pagina web.
se agrega una sección "proximamente" ya que contenido anterior era menor al necesario para ver operar el boton.
